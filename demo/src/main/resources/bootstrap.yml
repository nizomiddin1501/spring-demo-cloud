spring:
  application:
    name: demo
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: nizomiddin
      password: persistent
  profiles:
    active: local
#    active: prod