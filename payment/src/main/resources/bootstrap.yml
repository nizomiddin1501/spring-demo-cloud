spring:
  application:
    name: payment
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: nizomiddin
      password: persistent
  profiles:
    active: local
#    active: dev
